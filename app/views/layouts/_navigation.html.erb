<nav class="bg-blue-600 text-white shadow-lg">
  <div class="container mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16">
      <div class="flex items-center">
        <a href="<%= root_path %>" class="flex-shrink-0 flex items-center">
          <span class="text-xl font-bold tracking-tight">Athlete Ace</span>
        </a>
        <div class="hidden sm:ml-6 sm:flex sm:space-x-8">
          <%= link_to 'Sports', sports_path, :class => "inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium #{active_class(sports_path)}" %>
          <%= link_to 'Leagues', leagues_path, :class => "inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium #{active_class(leagues_path)}" %>
          <%= link_to 'Teams', teams_path, :class => "inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium #{active_class(teams_path)}" %>
          <%= link_to 'Players', players_path, :class => "inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium #{active_class(players_path)}" %>
          <%= link_to 'Stadia', stadia_path, :class => "inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium #{active_class(stadia_path)}" %>
          <%= link_to 'Countries', countries_path, :class => "inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium #{active_class(countries_path)}" %>
          <%= link_to 'States', states_path, :class => "inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium #{active_class(states_path)}" %>
          <%= link_to 'Cities', cities_path, :class => "inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium #{active_class(cities_path)}" %>
        </div>
        <div class="hidden sm:ml-6 sm:flex sm:space-x-8">
          <%= link_to 'Quests', quests_path, :class => "inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium #{active_class(quests_path)}" %>
        </div>
      </div>
    </div>
  </div>
</nav>
