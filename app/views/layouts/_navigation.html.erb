<nav class="bg-blue-600 text-white shadow-lg">
  <div class="container mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16">
      <div class="flex items-center">
        <a href="<%= root_path %>" class="flex-shrink-0 flex items-center">
          <span class="text-xl font-bold tracking-tight">Athlete Ace</span>
        </a>
        <div class="hidden sm:ml-6 sm:flex sm:space-x-8">
          <a href="<%= leagues_path %>" class="inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium <%= active_class(leagues_path) %>">
            Leagues
          </a>
          <a href="<%= teams_path %>" class="inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium <%= active_class(teams_path) %>">
            Teams
          </a>
          <a href="<%= players_path %>" class="inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium <%= active_class(players_path) %>">
            Players
          </a>
        </div>
      </div>
    </div>
  </div>
</nav>
